// Fungsi untuk pindah ke halaman login setelah animasi selesai
window.addEventListener('load', () => {
    setTimeout(() => {
        window.location.href = 'login.html'; // Ganti 'login.html' dengan nama file halaman login Anda
    }, 3000); // 3000 ms = 3 detik
});
